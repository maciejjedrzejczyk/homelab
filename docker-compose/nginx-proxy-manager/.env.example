# Nginx Proxy Manager host configuration.
# Copy to `.env` and adjust for your host.

# Host ports for HTTP, HTTPS, and the admin panel.
NPM_HTTP_PORT=80
NPM_HTTPS_PORT=443
NPM_ADMIN_PORT=17413

# Time zone.
TZ=Europe/Warsaw
